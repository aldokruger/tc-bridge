import { makeFinding } from "./_helpers.js";

/**
 * Regras de AWC search e indexação (plano §7.8).
 */
export function checkAWCSearch(ctx) {
	const findings = [];
	// AWC search checks requerem entidades de constantes de indexação.
	// Implementação mínima — verifica se constantes de search existem.
	return findings;
}
