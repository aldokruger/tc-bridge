# Copie para ".env" na maquina de upgrade e preencha os valores.
# Gere um token forte:  python -c "import secrets; print(secrets.token_urlsafe(48))"
TC_TOKEN=troque-por-um-token-forte-e-aleatorio
TC_PORT=4100

# Leitura: obrigatorio. Restrinja aos diretorios de trabalho necessarios.
TC_ALLOWED_READ_PATHS=D:\\upgrade;D:\\logs

# Escrita (opcional): descomente para permitir.
# TC_ALLOW_WRITE=1
# TC_ALLOWED_WRITE_PATHS=D:\upgrade;D:\logs

# Diagnosticos PowerShell (opcional). Nao aceita comandos arbitrarios.
# TC_ALLOW_DIAGNOSTICS=1
# TC_DIAGNOSTIC_HOSTS=localhost;127.0.0.1;::1

# Diagnosticos MSSQL somente leitura (opcional). Use uma conta dedicada de minimo privilegio.
# Nunca envie esta senha ao cliente MCP nem a registre em logs.
# TC_ALLOW_DB_DIAGNOSTICS=1
# TC_DB_SERVER=172.18.2.51
# TC_DB_PORT=1433
# TC_DB_NAME=tc_DEV_2026
# TC_DB_USER=tc_bridge_diagnostic
# TC_DB_PASSWORD=<segredo-fornecido-pelo-DBA>
# TC_DB_ENCRYPT=true
# TC_DB_TRUST_SERVER_CERTIFICATE=false

# Consultas Teamcenter SOA somente leitura (opcional).
# A conta deve ser exclusiva, com permissao minima e sem capacidade de manutencao.
# TC_ALLOW_TEAMCENTER_READ=1
# TC_TEAMCENTER_URL=http://localhost:8080/tc
# TC_TEAMCENTER_USER=tc_bridge_reader
# TC_TEAMCENTER_PASSWORD=<segredo-fornecido-pelo-administrador>
# TC_TEAMCENTER_GROUP=<grupo-opcional>
# TC_TEAMCENTER_ROLE=<role-opcional>
# TC_TEAMCENTER_LOCALE=en_US
# TC_TEAMCENTER_JAVA=E:\PLM\JAVA_JRE\bin\java.exe
# Deixe vazio para o cliente SOA negociar com o servidor; use Cp1252 ou UTF-8 somente quando confirmado no syslog do tcserver.
# TC_TEAMCENTER_SOA_CLIENT_ENCODING=Cp1252
# TC_TEAMCENTER_SOA_LIB=E:\PLM\Teamcenter2606\TcFTSIndexer\lib
# JARs extras exigidos pelo cliente SOA, separados por ; (por exemplo, Log4j).
# TC_TEAMCENTER_SOA_EXTRA_JARS=E:\PLM\Teamcenter2606\tccs\third_party\log4j2\2.25.3\log4j-api-2.25.3.jar;E:\PLM\Teamcenter2606\tccs\third_party\log4j2\2.25.3\log4j-core-2.25.3.jar
# TC_TEAMCENTER_SOA_ADAPTER_JAR=E:\PLM\tc-bridge\tc-bridge-soa-adapter.jar

# Autorizacao granular por action (opcional). O master switch
# TC_ALLOW_TEAMCENTER_READ habilita apenas preflight/health; preferences,
# objects e queries exigem flag granular explicita (nao herdam do master
# switch). Datasets e FMS ficam desligados por padrao ate homologacao.
# TC_ALLOW_TEAMCENTER_SOA_PREFLIGHT=1
# TC_ALLOW_TEAMCENTER_SOA_HEALTH=1
# TC_ALLOW_TEAMCENTER_SOA_PREFERENCES=1
# TC_ALLOW_TEAMCENTER_SOA_OBJECTS=1
# TC_ALLOW_TEAMCENTER_SOA_QUERIES=1
# TC_ALLOW_TEAMCENTER_SOA_DATASETS=0
# TC_ALLOW_TEAMCENTER_SOA_FMS=0

# Policy local obrigatoria para as acoes que exigem perfil (preferences/objects/
# queries/dataset/fms). JSON negado por padrao: sem arquivo, essas acoes falham.
# Exemplo em docs/soa-policy.example.json.
# TC_TEAMCENTER_SOA_POLICY_FILE=E:\PLM\tc-bridge\soa-policy.json

# Registro local de ambientes Teamcenter conhecidos (Fase 1). Cada perfil carrega
# identidade imutavel (environmentId), classificacao QA/PRD e componentes esperados;
# paths/URLs/credenciais permanecem na configuracao local protegida. Perfis invalidos
# sao reportados sem derrubar o agente. Exemplo em docs/environments.example.json.
# TC_ENVIRONMENT_REGISTRY_FILE=E:\PLM\tc-bridge\environments.json

# Controles de carga sobre o adaptador SOA (opcionais).
# TC_TEAMCENTER_SOA_MAX_CONCURRENCY=1
# TC_TEAMCENTER_SOA_QUEUE_LIMIT=4
# TC_TEAMCENTER_SOA_RATE_LIMIT=30
# TC_TEAMCENTER_SOA_TIMEOUT_MS=30000
# TC_TEAMCENTER_SOA_REQUIRE_TLS=0
# TC_TEAMCENTER_SOA_TRUST_STORE=

# Diagnosticos Chrome DevTools somente leitura (opcional).
# O Chrome deve ser iniciado localmente com --remote-debugging-port=9222.
# Nunca exponha a porta 9222 via tunnel, NAT ou firewall.
# TC_ALLOW_BROWSER_DIAGNOSTICS=1
# TC_BROWSER_DEVTOOLS_URL=http://127.0.0.1:9222

# Leitura de logs Teamcenter (opcional). Restrita a esta pasta, somente por capability autorizada.
# TC_ALLOW_LOG_READ=1
# TC_TEAMCENTER_LOG_DIR=C:\Users\siemens.plm\Siemens\logs

# Tarefas zero-trust assinadas por capability Ed25519 (opcional).
# A chave privada permanece somente no broker cloud; este host recebe apenas a chave publica.
# TC_ALLOW_CAPABILITY_TASKS=1
# TC_ENFORCE_CAPABILITIES=1
# TC_AGENT_ID=srv26-tc1-dev
# TC_CAPABILITY_PUBLIC_KEY=E:\PLM\security\tc-agent-capability-public.pem
# TC_CAPABILITY_ISSUER=https://broker.exemplo.com
# TC_AUDIT_LOG_PATH=E:\PLM\tcdata2606\logs\tc-agent-audit.jsonl

# Canal reverso mTLS (tc-agent). Certificados devem vir da PKI corporativa.
# TC_BROKER_URL=wss://broker.exemplo.com/agent
# TC_AGENT_CERTIFICATE=E:\PLM\security\tc-agent-cert.pem
# TC_AGENT_PRIVATE_KEY=E:\PLM\security\tc-agent-key.pem
# TC_BROKER_CA=E:\PLM\security\broker-ca.pem

# Broker de desenvolvimento/homologacao. Nunca use certificados autoassinados em producao.
# TC_BROKER_PORT=8443
# TC_BROKER_TLS_KEY=/run/secrets/broker-key.pem
# TC_BROKER_TLS_CERTIFICATE=/run/secrets/broker-cert.pem
# TC_BROKER_CLIENT_CA=/run/secrets/agent-client-ca.pem

# API MCP cloud do broker. Exponha apenas via HTTPS e mantenha o token e a chave privada em secret manager.
# TC_BROKER_API_PORT=8444
# TC_BROKER_API_TOKEN=<token-forte-exclusivo-da-api-mcp>
# Use um certificado publicamente confiavel para o cliente MCP. Se omitidos, reutiliza o certificado mTLS do broker (somente homologacao).
# TC_BROKER_API_TLS_KEY=/run/secrets/broker-api-key.pem
# TC_BROKER_API_TLS_CERTIFICATE=/run/secrets/broker-api-cert.pem
# TC_CAPABILITY_PRIVATE_KEY=/run/secrets/capability-private.pem
# TC_CAPABILITY_ISSUER=https://broker.exemplo.com
# TC_BROKER_ALLOWED_ACTIONS=browser.status;browser.pages;browser.capture_diagnostics;browser.performance;diagnostic.run;database.diagnostic;teamcenter.soa.preflight;teamcenter.soa.connection_health;teamcenter.soa.session_context;teamcenter.soa.preferences.read;teamcenter.soa.object.inspect;teamcenter.soa.saved_query.execute;teamcenter.logs.read
# TC_CAPABILITY_TTL_SECONDS=60
# TC_BROKER_SUBJECT=codex-service

# Tunel (opcional): localtunnel (padrao) | static
# TC_TUNNEL=localtunnel
# TC_PUBLIC_URL=https://seu-dominio-publico.example
